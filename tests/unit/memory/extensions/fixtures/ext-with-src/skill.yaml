id: with-src
name: With Src
category: extension
kind: command-skill
summary: A fixture that splits code into a src/ package, exercising the
  loader's sys.path support.
entrypoint:
  module: extension
runtimes:
  pi:
    command_name: ext-with-src
  claude:
    command_name: "ext:with-src"
